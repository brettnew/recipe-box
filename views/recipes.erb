<h2>Recipes</h2>
<hr>
<br>

<% if @recipes == [] %>

<p>There are currently no recipes in the database</p>

<% else %>

<p>Click on a recipe's name to view recipes and to add new ingredients and instructions OR click 'Edit' to edit recipe name or instructions, or to delete entire recipe. </p>
<ul>

  <% @recipes.order('rating DESC').each() do |recipe| %>

  <li><a href="/recipes/<%= recipe.id() %>"><%= recipe.name() %></a>
    <% if recipe.rating == 0 %>
      <strong>Unrated<strong><br><a href="/recipes/<%= recipe.id() %>/edit">Edit</a></li>
    <% else %>
       <strong><%= recipe.rating() %> stars</strong><br><a href="/recipes/<%= recipe.id() %>/edit">Edit</a></li>
     <% end %>
  <% end %>

</ul>

<% end %>

<h3> Add a recipe: </h3>
<form action="/recipes" method="post">
  <label for="recipe_name">Recipe Name:  </label>
  <input type="text" name="recipe_name" id="recipe_name" >
  <button type="submit">Add Recipe</button>
</form>
